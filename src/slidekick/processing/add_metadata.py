import os
from pathlib import Path
from typing import Tuple, Annotated

import numpy as np
import tifffile
import zarr

from slidekick.console import console
from slidekick.metadata import Metadata

def add_metadata(image, metadata: Metadata) -> Metadata:

    image_type = con


def add_stain_info():

def
